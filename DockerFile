FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

ENTRYPOINT ["python", "-m", "src.score_events"]

CMD ["--input", "data/beacon_events_eval_unlabeled.csv", "--output", "/app/results/eval_scored.csv"]